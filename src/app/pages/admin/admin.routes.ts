import { Routes } from '@angular/router';

// Admin sub-routes — Phase 1 (to be built)
export const adminRoutes: Routes = [
  {
    path: '',
    redirectTo: 'usuarios',
    pathMatch: 'full',
  },
];
